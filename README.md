# Docker PIA Proxy

A docker-compose file that sets up multiple services:
* A PIA VPN connection
* A Squid http proxy
* A tor relay

### Build
```
docker-compose build
```

### Run
```
docker-compose up -d
```

